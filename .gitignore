# ADNI data — governed by data use agreement, do not publish
data/*.csv
data/Tau_PET_Images/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# Generated outputs (run main.py to reproduce)
results/metrics.json

# Checkpoints — commit small model artifacts needed by the API, exclude large NN weights
checkpoints/*.pt
!checkpoints/logreg_combined.joblib
!checkpoints/shap_background.npy

# Plots — commit so the dashboard and notebook can reference them without retraining
# (gitignore negation requires the parent not to be ignored)
!results/plots/
!results/plots/*.png

# Claude Code
.claude/
